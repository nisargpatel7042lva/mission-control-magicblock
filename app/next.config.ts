import type { NextConfig } from "next";

// Next.js 16 removed the `eslint` build-config option entirely - `next
// build` no longer runs linting at all (lint is a separate `eslint` CLI
// step now), so there's nothing to configure here for that.
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
